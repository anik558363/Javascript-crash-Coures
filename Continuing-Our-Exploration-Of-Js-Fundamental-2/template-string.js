const myPet = {
    name : 'Travis',
    age : 2,
    isMale : true,
    food : "Fish",
    color: "white",
    'is married' : false,
    
}

const sentences = `My pet name is ${myPet.name}. His favorite food is ${myPet.food}. and his color is ${myPet.color}`;

console.log(sentences);
